# Claude Memory API Environment Variables

# Google Cloud Project Configuration
# Set these to your actual Google Cloud project details
PROJECT_ID=your-gcp-project-id
REGION=us-east4
SERVICE_NAME=claude-memory-api

# Anthropic API Configuration
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Application Configuration
NODE_ENV=production
PORT=8080

# Database Configuration
DB_PATH=/app/data/memory.db

# Cloud Storage Configuration
# This will be automatically set to {PROJECT_ID}-memory-data if not specified
BUCKET_NAME=your-gcp-bucket-name

# Setup Instructions:
# 1. Copy this file to .env
# 2. Replace 'your-gcp-project-id' with your actual Google Cloud Project ID
# 3. Replace 'your-anthropic-api-key-here' with your Anthropic API key
# 4. Replace 'your-gcp-bucket-name' with your desired bucket name (optional)
# 5. Adjust REGION if needed (default: us-east4)
# 6. Run: ./scripts/setup.ps1 to initialize the project
# 7. Run: ./scripts/deploy.ps1 to deploy to Google Cloud Run